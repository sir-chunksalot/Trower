using UnityEngine;

public class Level1_2 : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        //Debug.Log("UNLOCKS: " + Unlocks.GetUnlockedTraps());
    }

    // Update is called once per frame
    void Update()
    {

    }
}
